# configure the MongoDB client for a single node cluster on localhost
quarkus.mongodb.connection-string = mongodb://localhost:27017
# mandatory if you don't specify the name of the database using @MongoEntity
quarkus.mongodb.database = localTest

# Se quiser silenciar absolutamente tudo do driver:
# quarkus.log.category."org.mongodb.driver".level=WARN

quarkus.application.name=quarkus-mongodb-api
